#pragma once
#include <unordered_map>
#include <utility>
#include <queue>
#include "HTree.h"

class System{
    private:
    bool isCompression;
    HTree huffmanTree;
    // two classes ?? (Encode and Decode) ?
    public:
};